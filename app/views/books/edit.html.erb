<body>
  <h1>Editing Book</h1>

  <% if @book.errors.any? %>
    <div id="error_explanation">
    <h2><%= @book.errors.count %>errors prohibited this book from being saved:</h2>
      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message  %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<!-- form_with部分 -->
  <%= form_with model:@book,local: true do |f| %>
    <div class="field ">
      <div>
        <p>Title</p>
        <%= f.text_field :title %>
      </div>
    </div>
    <div class="field">
      <div>
        <p>body</p>
        <%= f.text_area :body %>
      </div>
    </div>
    <div class="actions">
      <%= f.submit 'Update Book' %>
    </div>
  <% end %>

<%= link_to "Show", book_path %>
<!--#showへパス-->
|
<%= link_to "Back", books_path %>
<!--book_pathのところをbook_path(book.id)にしなくていいのか？？？？？？-->
<!--# destroyへのパス-->
</body>